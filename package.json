{
  "name": "hawkenterprises.github.io",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "export": "next build && next export -o build && echo true >> build/.nojekyll",
    "deploy": "npm run export && gh-pages -d build"
  },
  "dependencies": {
    "next": "9.5.2",
    "react": "16.13.1",
    "react-dom": "16.13.1",
    "sass": "^1.26.10"
  },
  "devDependencies": {
    "gh-pages": "^3.1.0"
  }
}
